# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# Node / frontend (added when the dashboard build starts)
node_modules/
dist/
.env
.env.local

# Runtime-generated audit ledger -- meant to start fresh each run, not be
# committed with whatever test data happened to be in it locally
data/audit_ledger.jsonl

# Personal prep documents -- for local study only, not part of the public submission
ARCHITECTURE_EXPLAINED.md
INTERVIEW_PREP.md
VIDEO_SCRIPT.md

# OS
.DS_Store

# Editors
.vscode/
.idea/

# frontend build output (already in Node section above, being explicit)
frontend/node_modules/
frontend/dist/
